{
  "name": "ganesh-provisions",
  "version": "1.0.0",
  "private": true,
  "description": "Ganesh Provisions grocery storefront with WhatsApp receipts",
  "scripts": {
    "start": "npm start --prefix server",
    "install:all": "npm install --prefix server"
  }
}
